# The Movies DB Search App

> The app is developed in React and connects to the TMDB API. 

## Setup

Requires [Node.js](https://nodejs.org/en/).

```bash
# Install app dependencies
npm install

# Run the app
npm start

# App runs on http://localhost:3000
```